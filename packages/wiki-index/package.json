{
  "name": "@personal-wiki/wiki-index",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "pnpm typecheck && pnpm test",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/wiki-index/src --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@personal-wiki/wiki-core": "workspace:*",
    "@personal-wiki/wiki-db": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
